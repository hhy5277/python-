a,b=(1,2)   相当于a=1 b=2;  同理 a,b=[1,2]

-----
Python数据类型转换：
    int(x [,base])    x -- 字符串或数字。base -- 进制数，默认十进制。
    float(x)
    str()             返回一个对象的string格式。
    tuple( seq )      tuple([1,2,3,4])->(1, 2, 3, 4)
                      tuple({1:2,3:4}) ->(1, 3)  针对字典 会返回字典的key组成的tuple
    list(s)           将序列 s 转换为一个列表
    set(s)            转换为可变集合
    dict(d)           创建一个字典。d 必须是一个序列 (key,value)元组。
    chr(x),ord(x),hex(x),oct(x)




